#!/bin/bash
TODOPATH=~/Notes/todo
cd $TODOPATH
ranger

#FILE=~/.todo.md
#
#
#if [[ "$@" == "" ]] ; then
#	bat -f --paging always --theme ansi-dark -p $FILE
#elif [[ "$1" == "-e" ]]; then
#	vim $FILE
#else
#	echo '* ' "$@" >> $FILE
#fi
