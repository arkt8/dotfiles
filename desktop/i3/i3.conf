# This fime has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

#exec feh --no-startup-id --bg-fill ~/.config/wallpaper.jpg
# exec ~/.config/desktop/load.sh
include $HOME/.cache/i3colors.conf

set $statusDir $HOME/.config/desktop/i3status

set $mod Mod4
set $alt Mod1
set_from_resource $colorbg i3wm.background #000000
set_from_resource $colorfg i3wm.foreground #000000
set_from_resource $color0 i3wm.color0 #000000
set_from_resource $color1 i3wm.color1 #000000
set_from_resource $color2 i3wm.color2 #000000
set_from_resource $color3 i3wm.color3 #000000
set_from_resource $color4 i3wm.color4 #000000
set_from_resource $color5 i3wm.color5 #000000
set_from_resource $color6 i3wm.color6 #000000
set_from_resource $color7 i3wm.color7 #000000
set_from_resource $color8 i3wm.color8 #000000
set_from_resource $color9 i3wm.color9 #000000
set_from_resource $color10 i3wm.color10 #000000
set_from_resource $color11 i3wm.color11 #000000
set_from_resource $color12 i3wm.color12 #000000
set_from_resource $color13 i3wm.color13 #000000
set_from_resource $color14 i3wm.color14 #000000
set_from_resource $color15 i3wm.color15 #000000

set $alpha   #00000000
set $ablack  #00000055
set $awhite  #ffffff08
set $unfocus #aaaaaa
set $dialog $HOME/.config/desktop/scripts/dialog.sh

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8
#set $font "-*-terminesspowerline-medium-*-*-*-20-*-*-*-*-*-*-*"
#set $font "pango:Victor Mono Nerd Font Bold 13.7"
#set $font "xft:TerminessTTF Nerd Font Mono:pixelsize=12"
#set $font "xft:Terminus (TTF) 8"
set $font "xft:Victor Mono 8.5"
font $font

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8
# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
# exec --no-startup-id nm-applet


######## KEYBINDINGS {{{

#### Windows keys {{{
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod


# kill focused window
bindsym $mod+Shift+c kill

bindsym $mod+r          exec --no-startup-id $dialog run
bindsym $mod+Menu       exec --no-startup-id $dialog wm_menu
bindsym $mod+space      exec --no-startup-id $dialog settings
bindSym $mod+Shift+Menu exec --no-startup-id $dialog theme
# bindsym $mod+period exec --no-startup-id x-terminal-emulator -e vim $HOME/Notes/todo.txt

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+ccedilla focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Control+j move left
bindsym $mod+Control+k move down
bindsym $mod+Control+l move up
bindsym $mod+Control+ccedilla move right

# alternatively, you can use the cursor keys:
bindsym $mod+Control+Left move left
bindsym $mod+Control+Down move down
bindsym $mod+Control+Up move up
bindsym $mod+Control+Right move right

bindsym $mod+h split h
bindsym $mod+v split v
bindsym $mod+f fullscreen toggle

bindsym $mod+q layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

bindsym $mod+Shift+f floating toggle
bindsym $mod+g focus mode_toggle

bindsym $mod+$alt+Up focus parent
bindsym $mod+$alt+Down focus child

# resize window (you can also use the mouse for that)
mode "Redimensionar" {
	# same bindings, but for the arrow keys
	bindsym Left resize shrink width 10 px or 10 ppt
	bindsym Down resize grow height 10 px or 10 ppt
	bindsym Up resize shrink height 10 px or 10 ppt
	bindsym Right resize grow width 10 px or 10 ppt

	# back to normal: Enter or Escape or $mod+r
	bindsym Return mode "default"
	bindsym Escape mode "default"
	bindsym $mod+r mode "default"
}

bindsym $mod+s mode "Redimensionar"
#### Windows keys }}}
#### Workspace keys {{{
# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1  "1"
set $ws2  "2"
set $ws3  "3"
set $ws4  "4"
set $ws5  "5"
set $ws6  "6"
set $ws7  "7"
set $ws8  "8"
set $ws9  "9"
set $ws0  "10"

# Transparency
bindsym $mod+XF86AudioLowerVolume exec --no-startup-id transset -a -m 0.5 --dec 0.05
bindsym $mod+XF86AudioRaiseVolume exec --no-startup-id transset -a -m 0.5 --inc 0.05

# switch to workspace

#workspace $ws1 output eDP-1
#workspace $ws1 gaps outer
#workspace $ws2 gaps outer
#workspace $ws3 gaps outer
#workspace $ws4 gaps outer
#workspace $ws5 gaps outer
#workspace $ws6 gaps outer
#workspace $ws7 gaps outer
#workspace $ws8 gaps outer
#workspace $ws9 gaps outer
#workspace $ws0 gaps outer


bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws0
bindsym $mod+Tab exec i3-msg focus output right
bindsym $mod+Shift+Left exec i3-msg workspace prev_on_output
bindsym $mod+Shift+Right exec i3-msg workspace next_on_output
#bindsym $mod+Shift+Tab exec i3-msg focus prev
#bindsym $mod+Tab exec i3-msg focus next
#bindsym $mod+s exec i3-msg fous output right

# move focused container to workspace
bindsym $mod+Control+1 move container to workspace number $ws1
bindsym $mod+Control+2 move container to workspace number $ws2
bindsym $mod+Control+3 move container to workspace number $ws3
bindsym $mod+Control+4 move container to workspace number $ws4
bindsym $mod+Control+5 move container to workspace number $ws5
bindsym $mod+Control+6 move container to workspace number $ws6
bindsym $mod+Control+7 move container to workspace number $ws7
bindsym $mod+Control+8 move container to workspace number $ws8
bindsym $mod+Control+9 move container to workspace number $ws9
bindsym $mod+Control+0 move container to workspace number $ws0

#### Workspaces keys }}}}
#### Session keys {{{
# reload the configuration file
bindsym $mod+Control+r reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r exec ~/.config/desktop/load.sh reload
bindsym $mod+$alt+e exec i3-msg exit

#### Session Keys }}}
#### Tools keys {{{
# start a terminal
bindsym $mod+Return exec xterm -fa "xft:Terminus (TTF):pixelsize=16"
bindsym $mod+Shift+Return exec xterm -fa "xft:Terminus (TTF):pixelsize=20"
bindsym $mod+c exec --no-startup-id $ROFI -show calc -modi calc
# A more modern dmenu replacement is rofi:

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym XF86AudioPrev   exec --no-startup-id spt playback -p
bindsym XF86AudioPause  exec --no-startup-id spt playback -t
bindsym XF86AudioNext   exec --no-startup-id spt playback -n
bindsym XF86Search      exec --no-startup-id x-terminal-emulator -e spt

bindsym $mod+slash exec --no-startup-id $HOME/git/cheatsheets/run.sh
bindsym $mod+n                exec --no-startup-id $dialog rename_window
bindsym $mod+Shift+n          exec --no-startup-id $dialog rename_workspace
bindsym $mod+Shift+apostrophe exec --no-startup-id $dialog mark_add
bindsym $mod+apostrophe       exec --no-startup-id $dialog mark_focus
#bindsym $mod+period           exec --no-startup-id yeahconsole ~/.bin/vim-notes
#bindsym $mod+Shift+apostrophe exec i3-input -F 'mark %s' -l 10 -P 'Add mark: '
#bindsym $mod+apostrophe exec i3-input -F '[con_mark="%s"] focus' -l 10 -P 'Go to mark: '
bindsym $mod+XF86AudioMute exec --no-startup-id screenoff

#### Tools keys}}}

#### Layout {{{
bindsym $mod+equal exec i3-msg 'gaps inner all plus 5'
bindsym $mod+minus exec i3-msg 'gaps inner all minus 5'

#### }}}

######## }}} KEYBINDINGS

######## BEHAVIOR {{{

workspace_auto_back_and_forth yes
show_marks yes
default_border pixel 0
default_floating_border pixel 0
#smart_gaps inverse_outer
gaps inner 30
gaps outer 0

for_window [class=".*"] title_format " %class " mark_format " %mark"
for_window [class=".*"] border pixel 0
for_window [class="i3-dialog"] floating enable

######## }}} BEHAVIOR

######## THEME {{{{{{

# OPAQUE
# class                 border   bg       fg        indictr  child_border
# client.focused          $color8   $color7   $color15  $color7  $color7
# client.focused_inactive $color0   $color8   $color0   $color8  $color8
# client.unfocused        $color0   $color0   $unfocus  $color0  $color0
# client.urgent           $alpha    $color0   $alpha    $alpha   $alpha
# client.placeholder      $alpha    $color0   $alpha    $alpha   $alpha
# client.background       $color0
#
# see i3colors.conf

#### BARS {{{
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#### }}} BARS
######## }}} THEME}}}
# vim: foldenable foldmarker={{{,}}} foldmethod=marker
